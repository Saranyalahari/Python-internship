# Task – Python Calculator (Function-Based)

This project implements a clean, menu-driven calculator using Python functions and best coding practices.

# Features
- Separate functions for add, subtract, multiply, and divide
- Uses parameters, return values, and default arguments
- Menu-based user interaction
- Handles division by zero safely
- Includes input validation and error handling
- Well-structured and readable code with docstrings

# File Included
- `calculator.py`

# How to Run
1. Open a terminal in the project directory
2. Execute the program:
python calculator.py
3. Choose an operation from the menu and enter numbers when prompted

# Testing
Each function can be tested independently in the Python shell:
```python
add(5, 3)
subtract(10, 4)
multiply(6, 2)
divide(8, 0)